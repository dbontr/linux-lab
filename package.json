{
  "name": "linux-lab",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check:runtime": "node --check public/runtime/qemu-host.js && node --check runtime-build/qemu/patch-networking.mjs && node --check runtime-build/qemu/package-network-licenses.mjs && node --check runtime-build/qemu/network/bundle.mjs",
    "check": "npm run check:runtime && npm run test && npm run build",
    "sync:distros": "node scripts/sync-distros.mjs",
    "verify:media": "node scripts/verify-built-media.mjs"
  },
  "dependencies": {
    "v86": "^0.5.458"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
